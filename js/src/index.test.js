var assert = require('chai').assert

import * as lukai from './index.js'

describe('lukai', function () {
  it('should should be able to serialize and load a model', function () {
    assert.equal([1, 2, 3].indexOf(4), -1)
  })
})
